#include <iostream>
#include <complex>
int main ()
{
 using namespace std;
 char snake;
 int run (1);
char printMap(2);
int initMap(3);
char move(int dx, int dy);
int update(4);
char changeDirection(char key);
int clearScreen(0);
char generatorFood(2);
char getMapValue(int value);
const int mapwidth = 20;
cout << run << printMap << initMap << move << update << changeDirection << clearScreen << generatorFood << getMapValue << endl;
system ("pause");
return 0;
}
