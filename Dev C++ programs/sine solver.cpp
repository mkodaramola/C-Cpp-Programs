#include <iostream>

int main()
{
	using namespace std;
	
	double koff;
	double lof;
	double asit;
	double kod;
	double gyan;
	double nig;
	double dor;
	double sef;
	double cok;
	double shak;
	double dak = 50;
	double smog;
	int k=20;
	do {
	cout << "Find the sine of_____" << endl;
	cin >> smog;
	
nig = gyan * gyan;
dor =gyan * gyan * gyan;
koff = smog + 1;		
lof = (0.78539816 * koff) / 45;	
gyan = -0.01745329;	
asit = (0.70710678 * lof) / 0.78539816;
shak = (smog / dak);
cout <<"answer = " << 0.00001+asit + (gyan * asit) - ((nig * asit) / 2) - ((dor * asit) / 6) << endl;
cout << " " << endl;
} while (k=20);
system ("pause");
return 0;
}

